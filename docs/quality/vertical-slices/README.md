# Vertical Slice Manifests

Create one JSON manifest per new player-facing feature from `../vertical-slice-template.json`.

A feature may be marked `complete` only when acquisition, processing, purpose, JEI, handbook, feedback, failure recovery, and tests are all complete and each layer names concrete evidence. `tools/validate_resources.py` rejects incomplete evidence hidden behind a complete status.
